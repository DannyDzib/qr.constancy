const { handleLogin } = require("clients/httpLogin/httpLogin")

/* describe("Http Login Test", () => {
  it("Login Test", async () => {
    const req = {
      email: "l16530486@cancun.tecnm.mx",
      password: "pruebas2022",
    }
    const login = await handleLogin(req)
    expect(login).toHaveProperty("status")
    expect(login).toHaveProperty("isAdmin")
    expect(login).toHaveProperty("userName")
    expect(login).toHaveProperty("token")
  })
})
 */